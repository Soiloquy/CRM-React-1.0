---
description: React 组件开发规范 - 组件结构、导出方式、状态管理模式
globs: "**/*.tsx"
alwaysApply: false
---

# React 组件规范

## 组件导出

页面和组件统一使用默认导出：

```tsx
export default function ProductList() {
  // ...
}
```

## 页面组件结构

页面遵循统一布局模式：

```tsx
export default function PageName() {
  const { data, isLoading } = useXxxHook()

  if (isLoading) return <Spin />

  return (
    <div>
      <div className="page-header">
        <h2>页面标题</h2>
      </div>
      {/* 页面内容 */}
    </div>
  )
}
```

## 状态管理选择

- **UI 状态**（侧边栏折叠、筛选条件）→ Zustand store
- **服务端数据**（产品列表、客户详情）→ React Query hooks
- **局部状态**（表单、弹窗开关）→ `useState`

## UI 组件

- 优先使用 Ant Design 组件：`Table`, `Card`, `Form`, `Modal`, `Button`, `Input`, `Select`, `Tag`, `Spin`, `Empty`
- 图表使用 Recharts：`ResponsiveContainer`, `PieChart`, `BarChart`, `LineChart`
- 图标使用 `@ant-design/icons`

## Props 定义

使用 `interface` 定义 Props，不使用 `type`：

```tsx
interface Props {
  data: Product[]
  loading?: boolean
}

export default function ProductTable({ data, loading }: Props) {
  // ...
}
```
